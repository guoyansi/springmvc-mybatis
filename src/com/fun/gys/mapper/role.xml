<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fun.gys.dao.IRoleDao">

  <insert id="insertRole" parameterType="com.fun.gys.bean.RoleBean">
  	insert into role 
  	(roleName,note)
  	values
  	(#{roleName},#{note})
  </insert>
  <delete id="deleteRole" parameterType="int">
  	delete from role where id=#{id}
  </delete>
  <select id="getRole" parameterType="int" resultType="com.fun.gys.bean.RoleBean">
	select id,role_name as roleName,note from role where id=#{id}  	
  </select>
  <select id="findRoles" parameterType="string" resultType="com.fun.gys.bean.RoleBean">
  	select id,role_name as roleName,note from role where role_name like concat('%',#{roleName},'%');
  </select>
  <update id="updateRole" parameterType="com.fun.gys.bean.RoleBean">
  	update role
  	<set>
  		<if test="roleName!=null">role_name=#{roleName},</if>
  		<if test="note !=null">note=#{note},</if>
  	</set>
  	where id=#{id}
  </update>
</mapper>